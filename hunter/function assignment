#include<stdio.h>
int main()
{
	int n=8,max;
	int a[100]={10,45,60,32,57,88,99,10};
	max=func(a,n);
	printf("The maximum:%d",max);
	return 0;
}
int func(int a[],int n)
{
	int i,temp;
	temp=a[0];
	for(i=1;i<n;i++)
	{
		if(a[i]>temp)
		{
			temp=a[i];
		}
	}return temp;
}
