#include<stdio.h>
#include<conio.h>
void main()
int low,high,i,flag;
printf("enter the two number");
scanf("%d%d",&high,&low);
printf("enter the number between two numbers");
while(low<high)
{
flag=0;
for(i=0)
